1、运行环境
1）在命令行下后者VS code下跑，Python需要3.5版本以上
2）需要利用ffmpeg命令，将其设置为环境变量或者在此目录添加ffmpeg.exe文件
3）QwebEngine需要编译后的，将原来的QwebEngine替换

2、代码说明
1）直接运行pc_client.py代码即可
2）web_server.py代码和其他html文件均是放在服务器上运行的。如果需要在其他服务器或者本地跑，需要将Webcocket对应地址更换。